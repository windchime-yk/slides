{
  "name": "slides",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "deno run --allow-run=pnpm --allow-read --allow-env scripts/dev.ts",
    "build": "rimraf dist && pnpm -r build",
    "preview": "deno run --allow-run=pnpm scripts/preview.ts",
    "export": "pnpm -r export"
  },
  "dependencies": {
    "@slidev/cli": "^0.49.0-beta.4",
    "@slidev/theme-default": "latest",
    "@slidev/theme-seriph": "latest",
    "vue": "^3.4.22"
  },
  "devDependencies": {
    "live-server": "^1.2.2",
    "playwright-chromium": "^1.43.1",
    "rimraf": "^5.0.5"
  }
}
